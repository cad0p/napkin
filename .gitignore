# Logs
*.log

# Dependencies
node_modules/

# TypeScript
*.tsbuildinfo

# Build output (not shipped; package uses bin/napkin.js + jiti on src/)
dist

# pnpm
# pnpm-lock.yaml IS committed for reproducibility.

# Benchmark data and results (download on demand)
bench/data/
bench/results/

# dotenv
.env
.env.local
.env.*.local

# Nanny
.nanny/

# Vault (local content)
.napkin/
thread-draft.md

# bun rebuilds (fork dropped bun builds; keep for accidental rebuilds)
*.bun-build
